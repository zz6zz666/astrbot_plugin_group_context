{
  "enable_image_recognition": {
    "type": "bool",
    "description": "是否启用群聊图片识别（关闭后完全忽略所有图片，包括合并转发中的图片）",
    "default": false
  },
  "image_carry_rounds": {
    "type": "int",
    "description": "控制携带图片的轮数，只保留最后N轮用户消息中的图片，将前面的图片转换为[图片]占位符，用于节省请求token",
    "default": 2
  },
  "image_caption": {
    "type": "bool",
    "description": "是否启用图片描述功能（需先启用图片识别。适用于聊天大模型无法进行图片识别的场景）\n描述图片的效果不如原生图片识别，建议仅在无法使用图片识别的聊天大模型时开启",
    "default": false
  },
  "image_caption_prompt": {
    "type": "string",
    "description": "图片描述提示词",
    "default": "请描述这张图片的内容"
  },
  "image_caption_provider_id": {
    "type": "string",
    "description": "用于图片描述的 Provider ID (留空则使用当前正在使用的 Provider)",
    "default": ""
  },
  "enable_forward_analysis": {
    "type": "bool",
    "description": "是否启用QQ合并转发消息分析（仅aiocqhttp平台，支持直接发送或回复引用）",
    "default": true
  },
  "enable_active_reply": {
    "type": "bool",
    "description": "是否启用主动回复功能",
    "default": false
  },
  
  "ar_possibility": {
    "type": "float",
    "description": "主动回复概率 (0.0-1.0)",
    "default": 0.1
  },
  "active_reply_prompt": {
    "type": "string",
    "description": "主动回复提示词文本",
    "default": "You are now in a chatroom. The chat history is as above. Now, new messages are coming. Please react to it. Only output your response and do not output any other information."
  },
  "normal_reply_prompt": {
    "type": "string",
    "description": "非主动回复提示词文本",
    "default": "You are now in a chatroom. The chat history is as above. Now, new messages are coming."
  },
  "conversation_rounds_limit": {
    "type": "int",
    "description": "保留的user/assistant对话轮数",
    "default": 6
  },
  "ar_whitelist": {
    "type": "list",
    "description": "主动回复白名单 (群号或用户ID列表,留空表示不限制)",
    "default": []
  },
  "enable_command_filter": {
    "type": "bool",
    "description": "是否启用群聊指令消息过滤（以指定前缀开头的消息将被忽略）",
    "default": true
  },
  "command_prefixes": {
    "type": "list",
    "description": "指令消息前缀列表",
    "default": ["/"]
  },
    "enable_private_control": {
    "type": "bool",
    "description": "是否启用私聊场景会话控制功能",
    "default": false
  },
  "private_conversation_rounds_limit": {
    "type": "int",
    "description": "私聊场景保留的user/assistant对话轮数",
    "default": 10
  },
  "private_image_carry_rounds": {
    "type": "int",
    "description": "私聊场景控制携带图片的轮数，只保留最后N轮用户消息中的图片",
    "default": 2
  }
}
